

/*$('body').on('click', 'add-to-cart', function (e) {
	e.preventDefault();
	const id = $(this).data('id')
	alert(id);
})*/

function() {
	$("add-to-cart").click(function () {
		var model = {};
		mode.Id = @Model.Id;

	})
}